<Page x:Class="Modules.Translation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Modules.Translation"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage" UseLayoutRounding="True">
    <Page.Resources>
        <RoutedCommand x:Key="Enter"></RoutedCommand>
        <RoutedCommand x:Key="Format"></RoutedCommand>

    </Page.Resources>
    <Page.CommandBindings>
        <CommandBinding Command="{StaticResource Enter}" Executed="CommandBinding_Executed"></CommandBinding>
        <CommandBinding Command="{StaticResource Format}" Executed="CommandBindingFormat_Executed"></CommandBinding>
    </Page.CommandBindings>
    <Grid>
        <Grid.Resources>
            <Style TargetType="TextBox">
                <Setter Property="Background" Value="#5000"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Foreground" Value="White"></Setter>
                <Setter Property="BorderThickness" Value="0"/>
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="txtSource" ToolTip="{Binding Path=Text, RelativeSource={RelativeSource Mode=Self}}">
            <TextBox.InputBindings>
                <KeyBinding Command="{StaticResource Enter}" Key="Enter"></KeyBinding>
                <MouseBinding Command="{StaticResource Format}"  Gesture="LeftDoubleClick"/>
            </TextBox.InputBindings>
        </TextBox>
        <GridSplitter Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="Transparent"></GridSplitter>
        <TextBox Grid.Column="2" IsReadOnly="True" x:Name="txtResult" ToolTip="{Binding Path=Text, RelativeSource={RelativeSource Mode=Self}}"></TextBox>
    </Grid>
</Page>
